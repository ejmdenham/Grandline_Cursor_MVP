# Mobile app env: use apps/mobile/.env only (Expo loads from app project root).
# That file is auto-generated by:
#   - terraform apply (infra/terraform/mobile_env.tf), or
#   - scripts/gen-env.sh (e.g. npm run start in apps/mobile).
# Do not commit .env (it is in .gitignore).
