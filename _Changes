1. Onboarding Screen
2*. confirmation screen (the email with the confirmation code is sent to the email but there is not confirmation logic on the front or backend)
3. Get Admin WEbpage writing to dynamodb table via API/lambda (currently not working)

4. Phase 4 — Join Race and Pre-Race Experience (mobile):
   - Join Race screen: invite code input, validation, races API (get by invite code), navigate to Map on success.
   - Race context: current race and race state (idle / pre-race / in-race / post-race).
   - Races service: getRaceByInviteCode, getRaceById (Bearer token from Cognito).
   - Race types: Race, Checkpoint, RaceState (types/race.ts).
   - Bottom sheet: collapsible container with peek label; PreRaceContent: race name, mode badges (Run/Bike), start-window countdown, “Start Race” CTA.
   - Map screen: when race joined, top overlay (race name, 0:00, checkpoint 0/N), checkpoint and finish markers, pre-race bottom sheet.
   - Placeholder assets: run.svg, bike.svg in assets/icons/; assets.ts comments for marker PNGs.

GHAs:
